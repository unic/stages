# DOM adapter

This compatibility route now points to the expanded DOM documentation.

Start with [mounting and lifecycle](/adapters/dom/mounting), then see
[native fields](/adapters/dom/native-fields),
[custom views](/adapters/dom/custom-views), [focus](/adapters/dom/focus), and
[accessibility](/adapters/dom/accessibility). Every public export is documented
in the [DOM API reference](/reference/dom).
