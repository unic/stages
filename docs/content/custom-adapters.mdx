# Custom adapters and test kit

This compatibility route now points to the expanded custom-adapter
documentation.

Start with the [adapter contract](/adapters/custom/contract), follow the
[framework walkthrough](/adapters/custom/framework-walkthrough), then use the
[test-kit conformance checklist](/adapters/custom/testing-with-test-kit). Every
public `@stages/test-kit` export is documented in the
[test-kit API reference](/reference/test-kit).
