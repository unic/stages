# React adapter

The React adapter documentation has moved to the task-oriented
[React lifecycle guide](/adapters/react/lifecycle). Existing links to `/react`
remain valid through this compatibility page.
