# Migrating from 0.x

Stages v1 intentionally has no compatibility layer for `react-stages` 0.x.
Use the [migration overview](/migration/from-0.x) to classify each API as
**Replace**, **Move**, or **Remove**, then migrate one controlled surface at a
time through the focused task pages.
