# Legacy React Stages 0.x

This subtree is the historical React Stages 0.x implementation. Modify it only when the task explicitly concerns 0.x.

- Do not copy v1 APIs into this subtree.
- Do not make v1 packages or applications import it.
- Use `npm run build` as the current minimum verification for a legacy change.
