const config = {
    fields: () => {
        return [
            {
                id: "field1",
                label: "Field 1",
                type: "text",
                isRequired: true
            }
        ];
    }
};

export default config;