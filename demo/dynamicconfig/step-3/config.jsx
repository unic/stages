const config = {
    fields: () => {
        return [
            {
                id: "field2",
                label: "Field 2",
                type: "text",
                isRequired: true
            }
        ];
    }
};

export default config;